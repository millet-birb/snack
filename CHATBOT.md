# SafeSnack 챗봇 개발 과정

이 문서는 SafeSnack 웹사이트 안에서 작동하는 **AI 간식 추천 챗봇**을
어떤 과정으로 만들었는지 단계별로 정리한 기록입니다.

---

## 1. 무엇을 만들었나

웹사이트 우측 하단에 떠 있는 **말풍선 버튼**을 누르면 대화창이 열리고,
사용자가 "천식 있는 아이 간식 추천해줘" 같은 질문을 하면
챗봇이 **실제 제품 데이터베이스를 검색**해서 진짜 제품을 추천해 줍니다.

- AI 모델: **OpenAI `gpt-4o-mini`**
- 단순 상담을 넘어, 앱의 실제 제품 DB와 연결된 **"제품 추천 챗봇"**

---

## 2. 전체 구조 (아키텍처)

챗봇은 화면에선 버튼 하나지만, 뒤에서는 3개의 부품이 협력합니다.

```
 [사용자]
    │  "천식 있는 아이 간식 추천해줘"
    ▼
┌─────────────────┐      ┌──────────────────┐      ┌──────────────┐
│  ① 채팅 UI       │ ───▶ │  ② 챗봇 API       │ ───▶ │  ③ OpenAI    │
│  (프론트엔드)     │      │  (백엔드)          │      │  gpt-4o-mini │
│  말풍선 + 대화창  │ ◀─── │  메시지 중계·검색  │ ◀─── │  답변 생성     │
└─────────────────┘      └──────────────────┘      └──────────────┘
                                 │  ▲
                                 ▼  │
                         ┌──────────────────┐
                         │  제품 DB 검색      │
                         │  get_products()   │
                         └──────────────────┘
```

| 부품 | 위치 | 역할 |
|------|------|------|
| ① 채팅 UI | `front/components/chat/chat-widget.tsx` | 버튼·대화창·메시지 표시 |
| ② 챗봇 API | `backend/app/routers/chat.py` | 메시지 중계, 제품 검색 실행 |
| ③ OpenAI | 외부 서버 | 문장 이해 및 답변 작성 |

**왜 백엔드(②)를 거치나?**
OpenAI를 쓰려면 API 키(비밀번호 같은 것)가 필요한데, 이 키를 프론트엔드
(브라우저)에 두면 누구나 훔쳐볼 수 있습니다. 그래서 키는 백엔드에만
숨겨두고, 프론트는 백엔드에게만 요청합니다.

---

## 3. 개발 과정 (6단계)

작은 단계로 쪼개서, 각 단계가 끝날 때마다 작동을 확인하며 진행했습니다.

### 1단계 — OpenAI API 키 발급
- <https://platform.openai.com> 가입
- **API keys** 메뉴에서 `sk-`로 시작하는 키 발급
- **Billing**에서 결제수단 등록, **Limits**에서 월 사용 한도 설정

### 2단계 — 키를 안전하게 저장
- `backend/.env` 파일을 만들어 키를 저장
  (`.env` = 비밀 값을 담는 파일)
- `.gitignore`의 `*.env` 규칙으로 git에 절대 올라가지 않도록 확인
- `backend/.env.example` — 실제 키 없는 견본 파일 (git에 올려도 안전)

### 3단계 — 백엔드에 채팅 엔드포인트 만들기
- `openai`, `python-dotenv` 패키지 설치
- `backend/app/routers/chat.py` 생성 — `POST /api/chat` 엔드포인트
- `backend/app/main.py`에 라우터 연결
- 이 단계에서는 **도구 없이** 일반 대화만 가능 (앱 데이터는 아직 못 봄)
- 터미널에서 테스트 → ChatGPT가 한국어로 답변하는 것 확인

### 4단계 — 제품 검색 도구 연결 (레벨 2, 핵심)
- OpenAI의 **Function Calling**(도구 사용) 기능 적용
- 챗봇에 `search_snacks`라는 도구를 쥐어줌
- 이 도구는 기존 검색 함수 `get_products()`를 **그대로 재사용**
- 이제 챗봇이 실제 제품 DB를 검색해서 진짜 제품을 추천
- 터미널 테스트 → "소아당뇨 아이 과자 추천" → 실제 제품명·가격으로 답변 확인

### 5단계 — 프론트엔드 채팅 UI 만들기
- `front/components/chat/chat-widget.tsx` 생성
  - 우측 하단 말풍선 버튼
  - 클릭하면 열리는 대화창 (메시지 목록 + 입력창)
  - 백엔드 `/api/chat`로 메시지 전송
- `front/components/snack-app.tsx`에 위젯을 추가 → 모든 화면에 표시

### 6단계 — 전체 연결 및 확인
- 백엔드 + 프론트엔드 서버를 동시에 실행
- 브라우저에서 실제로 대화 → 정상 작동 확인

---

## 4. 핵심 동작 원리: Function Calling (도구 사용)

레벨 2 챗봇의 핵심입니다. AI 모델은 똑똑하지만 **우리 제품 DB는 못 봅니다.**
그래서 다음 흐름으로 동작합니다.

```
1. 사용자: "천식 있는 아이 간식 추천해줘"
2. 백엔드 → OpenAI에 메시지 전달
   + "너에겐 search_snacks 라는 도구가 있어" 라고 알려줌
3. OpenAI: "직접 답 못 하겠고, search_snacks(천식) 를 써야겠다" 라고 회신
4. 백엔드가 실제로 검색 실행 → get_products(conditions=["소아천식"])
5. 검색 결과(실제 제품 목록)를 다시 OpenAI에 전달
6. OpenAI: 결과를 보고 자연스러운 추천 문장 작성
7. 사용자 화면에 표시
```

이 과정은 `chat.py`의 `chat()` 함수 안에서 **최대 4번까지 반복**되는
루프로 구현되어 있습니다. (도구를 안 쓰면 바로 답변 반환)

관련 코드:
- `SEARCH_SNACKS_TOOL` — OpenAI에 알려줄 도구 설명
- `run_search_snacks()` — 도구가 호출되면 실제로 실행되는 함수

---

## 5. 만들어진 / 수정된 파일

| 파일 | 종류 | 내용 |
|------|------|------|
| `backend/.env` | 신규 | OpenAI API 키 (비밀, git 제외) |
| `backend/.env.example` | 신규 | 키 견본 파일 |
| `backend/requirements.txt` | 수정 | `openai`, `python-dotenv` 추가 |
| `backend/app/routers/chat.py` | 신규 | **챗봇 엔드포인트 (핵심)** |
| `backend/app/main.py` | 수정 | 챗봇 라우터 연결 |
| `front/components/chat/chat-widget.tsx` | 신규 | **채팅 UI (핵심)** |
| `front/components/snack-app.tsx` | 수정 | 위젯을 화면에 표시 |

---

## 6. 실행 방법

터미널 2개를 열어 백엔드·프론트엔드를 각각 실행합니다.

```powershell
# 터미널 1 — 백엔드 (FastAPI, 포트 8000)
cd snack-safe-app\backend
python -m uvicorn app.main:app --port 8000

# 터미널 2 — 프론트엔드 (Next.js, 포트 3000)
cd snack-safe-app\front
pnpm dev
```

그 다음 브라우저에서 <http://localhost:3000> 접속.

> ⚠️ 백엔드 코드(`chat.py` 등)를 수정하면 백엔드 서버를 **재시작**해야
> 변경이 반영됩니다 (`Ctrl+C`로 끄고 다시 실행).

---

## 7. 사용한 모델 / 모델 바꾸는 법

- 현재 모델: **`gpt-4o-mini`** (저렴하고 빠름)
- 모델은 `backend/app/routers/chat.py`의 `MODEL` 변수 **한 줄**에서 정해집니다:
  ```python
  MODEL = "gpt-4o-mini"
  ```
- 다른 모델로 바꾸려면 이 값만 수정 후 백엔드 재시작
  (예: `"gpt-4o"` — 더 똑똑하지만 더 비쌈)
- `gpt-4o-mini`는 "별명"이고, 실제로는 그 시점의 최신 버전
  (예: `gpt-4o-mini-2024-07-18`)에 연결됩니다.

---

## 8. 비용

- OpenAI API는 **사용한 만큼** 과금됩니다.
- `gpt-4o-mini`는 저렴해서 개발/학습 단계에는 부담이 적습니다.
- 도구를 쓰는 대화는 OpenAI와 2번 왕복하므로 응답에 수 초가 걸리고
  비용도 일반 대화보다 약간 더 듭니다.
- OpenAI 사이트의 **Limits**에서 월 사용 한도를 걸어두면 안전합니다.

---

## 9. 토큰(비용) 절감 — 적용한 최적화

`gpt-4o-mini`가 저렴해도 대화가 반복될수록 토큰 비용이 쌓입니다.
다음 다섯 가지 최적화를 `backend/app/routers/chat.py`에 적용해 평균 토큰
사용량을 크게 줄였습니다.

| 최적화 | 코드 위치 | 효과 |
|--------|----------|------|
| 히스토리 자르기 (최근 6개) | `MAX_HISTORY = 6` | 대화가 길어져도 토큰 선형 증가를 막음 |
| 도구 결과 필드 축소 | `run_search_snacks()` | 제품당 8필드 → 5필드 (필수만 유지) |
| 1턴 이후 `tools` 제거 | `if i == 0: kwargs["tools"] = [...]` | 27개 enum이 든 도구 스키마를 매 턴마다 재전송하지 않음 |
| 응답 길이 상한 (`max_tokens=500`) | `client.chat.completions.create(...)` | 출력 토큰 비용 캡 |
| 동일 질문 응답 캐시 | `_RESPONSE_CACHE` (LRU 128개) | 같은 질문이면 OpenAI 호출 없이 즉시 응답 |

### 응답 캐시 동작 방식

1. 사용자가 질문하면 메시지 텍스트를 정규화(`strip().lower()` + 공백 압축)해
   캐시 키로 사용합니다.
2. 키가 캐시에 있으면 OpenAI를 호출하지 않고 저장된 답을 그대로 돌려줍니다 (비용 0).
3. 없으면 OpenAI에 물어보고, 최종 답이 나오면 캐시에 저장합니다.
4. 캐시가 128개를 넘으면 가장 오래 쓰지 않은 항목부터 버립니다(LRU).

주의사항:
- 캐시는 **메모리에만** 저장됩니다 — 백엔드 서버를 재시작하면 사라집니다.
- `uvicorn --workers N` 으로 여러 워커를 띄우면 워커마다 캐시가 따로 생깁니다.
- 캐시 키는 **현재 메시지만** 보고 히스토리는 고려하지 않습니다 —
  "왜 그래?" 같은 짧고 모호한 후속 질문은 직전 맥락과 무관하게 같은 답이 나갈 수 있습니다.
- 영속화가 필요하면 Redis/SQLite 같은 외부 저장소로 옮겨야 합니다.

---

## 10. 개발 중 만난 문제와 해결

| 문제 | 원인 | 해결 |
|------|------|------|
| 포트 8000 사용 중 오류 | 이전 백엔드 서버가 아직 실행 중 | 기존 프로세스 종료 후 재시작 |
| 한글 JSON 전송 시 깨짐 | 터미널 인코딩 문제 | 한글을 파일로 저장해 전송하여 테스트 |

---

## 11. 앞으로 개선하면 좋을 점

- **추천 제품을 카드로 표시** — 글자 대신 제품 이미지·상세 링크 포함
- **응답 스트리밍** — 답변을 한 글자씩 흘려보내 체감 속도 개선
- **배포 전 보안** — `backend/app/main.py`의 CORS `allow_origins=["*"]`를
  실제 서비스 도메인으로 좁히기
- **대화 기록 저장** — 현재는 새로고침하면 대화가 초기화됨
- **응답 캐시 영속화** — 현재 캐시는 메모리에만 있어 서버 재시작 시 사라짐.
  Redis 등으로 옮기면 워커 간 공유 + 재시작 후에도 유지 가능
